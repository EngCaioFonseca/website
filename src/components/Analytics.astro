---
import { SERVICES } from '../consts';
const code = SERVICES.goatcounterCode;
---

{
  code && (
    <script
      is:inline
      data-goatcounter={`https://${code}.goatcounter.com/count`}
      async
      src="//gc.zgo.at/count.js"
    />
  )
}
